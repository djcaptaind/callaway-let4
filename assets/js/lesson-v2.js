const sections=[...document.querySelectorAll('[data-progress]')],bar=document.getElementById('progressBar'),label=document.getElementById('progressLabel');function updateProgress(){let p=0,t=scrollY+innerHeight*.55;sections.forEach(s=>{if(s.offsetTop<=t)p=Number(s.dataset.progress||0)});if(bar)bar.style.width=p+'%';if(label)label.textContent=p+'%'}addEventListener('scroll',updateProgress,{passive:true});updateProgress();const sf=document.getElementById('scenarioFeedback');document.querySelectorAll('.choice').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.choice').forEach(x=>x.classList.remove('correct','incorrect'));let c=b.dataset.correct==='true';b.classList.add(c?'correct':'incorrect');sf.textContent=b.dataset.feedback||''}));const qf=document.getElementById('quizFeedback');document.querySelectorAll('.quiz-options button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.quiz-options button').forEach(x=>x.classList.remove('correct','incorrect'));let c=b.dataset.answer==='correct';b.classList.add(c?'correct':'incorrect');qf.textContent=c?'Correct. Accountability means owning the situation and taking action to improve it.':'Try again. Look for the response that accepts responsibility and moves toward a solution.'}));
